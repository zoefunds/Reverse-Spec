{
  "name": "reverse-spec-relayer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bridges Base Sepolia USDC escrow (ReverseSpecEscrow.sol) and the GenLayer ReverseSpecBounties contract. GenLayer judges; this relayer confirms funding one direction and relays payout instructions the other. Never decides who gets paid itself.",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "ethers": "^6.13.0",
    "genlayer-js": "^0.16.0"
  }
}
