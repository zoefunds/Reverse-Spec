# VOICEOVER_SCRIPT — 90 s master (~200 words ≈ 135 wpm)

Neutral, confident voice. `//` = beat pause.

1. (0.0–4.5) Every bounty starts with a spec. // And sometimes — the spec is wrong.
2. (4.5–15) Traditional bounties pay for compliance. Build the widget, tick the boxes, collect the reward. // But the developer who finds the real problem behind the spec? No server, no company, can fairly judge that claim.
3. (15–22) ReverseSpec inverts the bounty. // The spec is a hypothesis. The deeper problem is the prize.
4. (22–40) A creator escrows real GEN into an Intelligent Contract, alongside the literal spec. // Solvers respond with a rationale — and a public evidence artifact. // Ignoring the spec is allowed. If the root cause gets solved.
5. (40–58) Here's what makes it trustless. // GenLayer's AI validators fetch each artifact themselves — inside consensus. They judge depth, superiority, and evidence quality — and agree on a verdict no single party controls.
6. (58–72) This is a real verdict, on-chain today. // Deep solution — depth ninety-one. Forty-seven and a half thousand GEN, paid out of the contract. // And a conservation invariant anyone can query.
7. (72–84) It runs live on GenLayer StudioNet — a deployed contract, a twenty-four-seven indexer, and a working claim path — designed to become native to mainnet.
8. (84–90) The spec is a hypothesis. // Pay for the truth.

Qualified claims: line 6 "on-chain today" = StudioNet chain state (truth-map row "Network status"); line 7 names StudioNet explicitly; line 7 "designed to become native to mainnet" is the approved future-facing formulation.
